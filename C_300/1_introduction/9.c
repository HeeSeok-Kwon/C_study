//주석 개념 배우기
// #include <stdio.h>

// int main()
// {
//   int i; //정수형 변수 i를 정의
//   int hap=0; //정수형 변수 hap을 정의, 0으로 초기화

//   // for문은 다음처럼 실행됩니다.i
//   // 1. i에 1을 초기값으로 설정합니다.
//   // 2. i가 10보다 작거나 같을 때까지만 순환문은 반복됩니다.
//   // 3. 현재의 hap의 값과 i 값을 더해 hap에 대입
//   // 4. i는 1씩 증가

//   for(i=1;i<=10;i++)
//   {
//     hap = hap + i;
//   }
//   //1부터 10까지의 합 55가 화면에표시
//   printf("1부터 10까지의 합 : %d", hap);

//   return 0;
// }